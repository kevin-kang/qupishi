require(['module/util'], function(util) {
    var $doc = $(document),
        $win = $(window),
        $jSubmitAsk = $('.j_submit_ask'),
        $selectCity = $('.an_tips_key_tips'),
        questionid = decodeURIComponent(util.query(location.href, 'question_id')),
        tmpArr;

    
});